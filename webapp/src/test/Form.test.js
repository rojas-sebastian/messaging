/* import { render, screen, fireEvent } from "@testing-library/react";
import Form from "../components/form/form";

describe("Form component", () => {
  let PhoneInput;
  let bodyMessage;

  beforeEach(() => {
    render(<Form></Form>);
    PhoneInput = screen.getByTitle("phone input");
    bodyMessage = screen.getByPlaceholderText(
      "Escribe un maximo de 160 caracteres"
    );
  });

  test("test true", () => {
    expect();
  });
});
 */
